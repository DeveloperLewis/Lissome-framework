{% extends "templates/main.twig" %}

{% block body %}
    <div class="text-white text-center mt-5">
        <h1 class="lato-light">Square <span class="lato-strong">Framework</span></h1>
        <h4 class="lato-light"><span class="lato-strong">Square</span> is a slim and fast php framework for building MVC
            applications. </h4>
    </div>

    {% if username != null %}
        <p class="text-center text-white">You are logged in, {{ username }}</p>
    {% endif %}
{% endblock %}

